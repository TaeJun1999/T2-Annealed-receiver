{"message": "Adversarial review of /home/HTJ/t2/conf/results/review_next/NEXT_EXPERIMENTS_P3.md (v1, currently untracked — not yet committed). Read-only; CPU checks in scratchpad.\n\nVERIFIED (not defects)\n- Leakage: no artefact touches trials >= 3200. Every raw_* dir tops out at skip 3160 (+40 = 3200 exclusive; raw_supp <= 600); p1_cavity_*.npz trials 2560..3199; grep for \"skip0 3200/skip3200\" over conf/logs, conf/results, conf/code, Demo hits only the P3 doc. Threshold 1e-3 is fixed in the diag_p3_loop.py docstring \"before looking at the numbers\"; 24 and +-3 first appear in the P3 doc; no judgement data exists. Oracle appears in no §3 rule.\n- Groundwork r_t is the receiver's actual h_post (V1 via the _matrix_site hook; nmse_dev == 0 for V1 and b*), so the wrapper in §5 measures the same quantity.\n- Bit-identity of iterations 1..16 of a 32-iteration run: RouteA.run (Demo/t2_route_a.py:321-435) has no n_iter dependence and consumes no RNG, so it holds by construction — except the raise path (item 6).\n- R32 vs R16 on one trajectory is a legitimate exact McNemar test on flips (deterministic pairing; a block decoded@16/failed@32 is a discordant pair against R32).\n- MDD, exact two-sided sign test, n=1280: n_d 38/51/64/77/90/102 (3..8%) -> 14/15/18/19/20/22 (doc says \"14~21\"; 22 at exactly 8%). The §6 prediction (15-25 net, lost 0-2, from rescued 11/13 & lost 0/1 on dev) implies n_d ~ 15-27 = 1.2-2.1% discordance, i.e. below the quoted 3-8% range, with p < 0.01 -> n=1280 is adequate for H6a. Report-only points (n=640) are correctly excluded (§3 header \"C2 −3 dB, n=1280\").\n- Empirically 0 raised and 0 guardH among 4480 V1/b* blocks at C2 −3 dB (raw_B16e4k, review_next_A2, H0 x2), so the NaN items below are code paths, not observed events.\n\nDEFECTS\n\n1. [HIGH] §2.4: \"루프 개선이 격차를 **줄이면**(GMM 이 더 많이 회복) \"이득의 일부는 루프 정체였다\", **유지·확대되면** \"루프와 무관한 prior 이득\" 으로 서술한다 — 어느 쪽이든 격차의 부호검정과 함께.\"\nDefect: no test or threshold for \"줄이면\". The gap's sign test (V1 vs b* under a rule) tests whether a gap exists, not whether it changed; §6 defines \"유지\" as p<0.05 while \"줄이면\" is a point comparison. Both narratives can be true at once (gap shrinks e.g. 44 -> 30 net and stays p<0.05) -> narrative chosen after seeing data, on the sentence that feeds the paper story. \"각 규칙\" is also unbounded (oracle uses true bits).\nFix: pre-declare the interaction test already used in v3 §3.2: per trial d_i = (fail_V1^rule − fail_V1^R16) − (fail_b*^rule − fail_b*^R16), exact two-sided sign test on d≠0, MIN_DISC 6. \"격차 일부는 루프 정체\" only if p<0.05 and #d>0 > #d<0; \"루프와 무관한 prior 이득\" only if p<0.05 the other way; otherwise \"판정 불가\". Rules = {R32, R-adapt, S-res}; oracle and S-conf excluded.\n\n2. [HIGH] §3: \"H6b | R-adapt vs R16 부호검정 p < 0.05 이고 실패 적음, **그리고** 평균 반복 수 < 24\"; \"H7 | 각 감쇠 변형 R16 vs 기본 R16 | 어느 변형이든 p < 0.05 로 실패가 적으면 H7 기각\"; \"H8 | S-res vs 기본 R32 부호검정\".\nDefect: only H6a states the decision unit (\"V1·b\\* 각각 ... 한쪽이라도 → 그 prior 에서 기각\"). H6b (2 tests), H7 (2 variants x 2 priors = 4), H8 (2) do not say per-prior / both / either, and \"평균 반복 수\" does not say which prior or which blocks. Ten decision tests plus §2.4 with the aggregation left open; H7 as \"any of 4\" has ~19% false rejection under the null.\nFix: \"H6b/H7/H8 are decided per prior exactly like H6a; a rule counts as adopted only when supported in both priors (§1 대칭성); 평균 반복 수 = mean R-adapt stop t over all 1280 blocks of that prior\". List the full test count and state no test is added.\n\n3. [MEDIUM] §2.2: \"H7: 어느 감쇠 변형도 ... 실패를 줄이지 못한다(부호검정 p ≥ 0.05 또는 더 많음)\"; §6: \"H7: 감쇠 변형은 판정 불가(p ≥ 0.05) → H7 유지\"; §3 H8: \"p ≥ 0.05 또는 더 많음\" (= 기각); §6: \"H8: S-res 는 R32 와 판정 불가(선택이 대부분 기본을 고름) → 기각\".\nDefect: non-significance is turned into a verdict, contradicting the same table (\"p ≥ 0.05 는 '이 설계로 판정 불가'\") and v3 §0 (\"차이 없음\" 이 아님). In the predicted H8 case (S-res mostly = base) n_d < 6 -> UNDECIDED by §1, yet the doc scripts \"기각\".\nConsequence: the paper can say \"damping / restart selection does not help\" from an underpowered or undecidable test.\nFix: three outcomes for every hypothesis — 지지 (p<0.05, fewer failures), 판정 불가 (p≥0.05, n_d≥6), UNDECIDED (n_d<6). H7 can only be \"기각\" or \"판정 불가\", never \"유지\"; §6 H8 \"→ 기각\" becomes \"→ 판정 불가/UNDECIDED\".\n\n4. [MEDIUM] §2.3: \"H8: S-res 는 R32(기본 단일 실행) 보다 실패가 적다\"; §2.2 computes \"규칙 R16/R32/R-adapt 를 모두 계산\" for the variants but H7 tests only \"각 감쇠 변형 R16 vs 기본 R16\".\nDefect: r_t near a fixed point scales with the damping (β on rD/tauD, β_fb on the feedback), so \"smallest r_32\" systematically tracks one configuration rather than \"the run that converged best\". If b05 or fb05 beats base at R32 — computed but never tested — H8 \"supported\" attributes a single-variant effect to observation-based selection, and H7 (R16 only) cannot catch it.\nFix: co-primary tests S-res vs b05-R32 and S-res vs fb05-R32 in addition to base-R32; H8 supported only if S-res beats every single run; report per-run selection frequency.\n\n5. [MEDIUM] §3: \"테스트 집합(0..2559)에서의 확증 실행 여부는 **사용자 결정**으로 넘긴다\".\nDefect: v3 §0 requires the confirmation's \"1차 비교·판정점·통과 기준\" to be written before the judgement data. Only go/no-go is delegated; if the user approves after seeing judgement results, comparison/points/pass rule are chosen post hoc. Headline itself is correctly untouched.\nFix: add now: primary = adopted rule (R32 or R-adapt) vs R16 for V1 and b* on test set 0..2559, C2 −3/0/+3 dB, 3-point rule + 08_SPEC §2 power guard, R16 reference = existing raw_B16e4k with bit-identical iterations 1..16 (same Demo hashes), one run, no headline change; the user decides only whether it runs.\n\n6. [MEDIUM-LOW] §2.1: \"R-adapt = 반복 t ∈ [16, 32] 중 처음으로 r_t < 1e-3 인 t 에서 멈춤(없으면 32)\"; §2.3: \"S-res = 32회째 r_32 가 가장 작은 실행 ... 동률이면 기본\"; §3: \"규칙은 발산 블록도 같은 식으로 다룬다\".\nDefect: r_t is NaN when h_post is non-finite or ‖h^t‖ = 0 (0/0); \"r_t < 1e-3\" is then False (never stops, cost 32) by numpy accident, not by rule; np.argmin over NaN returns the first NaN so S-res can pick a diverged run, and NaN never ties so \"동률이면 기본\" cannot fire. A block whose arm raises at t > 16 gets an all-NaN log (conf/code/runner.py:439-441) which load_raw rewrites to blk_err = 1 at every iteration (conf/code/analysis.py NaN-row block): R16 from the 32-run then differs from the 16-iteration reference (acceptance check fails) and a genuine loss (decoded@16, raised later) becomes a concordant failure; stop t and mean iteration count are undefined.\nFix (one line): \"non-finite r_t counts as ≥ threshold (never stops; S-res treats it as +inf); a block raised after iteration 16: R16 from the 16-iteration reference run, R32/R-adapt = failure, cost = 32\".\n\n7. [LOW-MEDIUM] §2.1: \"H6b: R-adapt 는 R16 보다 실패가 적고(부호검정), R32 와의 순 차이는 ±3 블록 이내(보고 전용 대역)이며 평균 반복 수 < 24\"; §6: \"H6b: R-adapt 는 R32 와 ±3 안, 평균 반복 ≈ 18~20 → 채택\"; §3: \"둘 중 하나라도 어긋남\" (two conditions).\nDefect: whether |net(R-adapt − R32)| > 3 blocks adoption is decided at read time. \"< 24\" cannot fail on dev-like data (84% of blocks settle by 16 -> mean ≤ 16 + 0.16·16 ≈ 18.6), so it adds nothing but the appearance of a cost bound.\nFix: §3 states \"±3 is report-only and not a condition\"; drop 24 or replace with a cost criterion that can fail.\n\n8. [LOW-MEDIUM] §0: \"settled (max r < 1e-3)\" (over 13..16) vs §2.1 single-iteration \"처음으로 r_t < 1e-3\".\nDefect: on dev C2 −3 dB, 19 V1 blocks (4 failed) and 11 b* blocks (6 failed) dip below 1e-3 exactly at t=16 after r_13..15 ≥ 1e-3; R-adapt stops them at 16 though they were moving one iteration earlier. §0's motivating class does not describe the rule and the predicted ±3 vs R32 is exposed to transient dips.\nFix (before commit): require r_t < 1e-3 on two consecutive iterations, or keep single-t and record that the 19/11 dev counts were seen.\n\n9. [LOW] §1: \"`runner.py run --skip0 3200 --n 1280`. 수용 검사: 점마다 정확히 {(3200+40k, 40) : k=0..31}\".\nDefect: runner.py refuses --skip0 ≠ 0 without --tag and the run needs --iters 32 (and --arm/--stagec-ckpt as applicable); analysis.load_raw's hole check assumes the first chunk is 0 or DEV_SKIP0=2560 (conf/code/analysis.py:129) so every judgement-set point is flagged \"holes/overlaps\"; pair_tags.py hardcodes DEV_CHUNKS (16 x 40). The acceptance check has no implementing tool.\nFix: write the full command; add a P3_SKIP0=3200 start to load_raw; put the {(3200+40k,40): k=0..31} check into p3_rules.py.\n\n10. [LOW] §2.2: \"기각되면 그 변형이 P3-C 의 재시작 후보가 된다\" vs §2.3 \"블록마다 {기본, b05, fb05} 세 실행 중 하나를 고른다\" (unconditional). Dead/contradictory clause. Fix: delete it.\n\n11. [LOW] §2.3 \"비용 = 실행 3개 = 3×\" vs §4 \"실행 3개(P3-C) = 6×\"; §4's estimate omits the 16-iteration reference run required by §5 row 3 and the two report-only points, and --iters 32 also runs R5-genie at 32 although §2.1 calls it a \"16 반복 참조\". Fix: one base (16-iteration single run = 1×) and list every run.\n\n12. [LOW] §2.3: \"S-conf = alphaD_32 가 가장 큰 실행 (보고 전용, 2차)\". \"2차\" reads as a fallback if S-res fails. Fix: \"S-conf, oracle and variant-R32 numbers are report-only and are never promoted to a decision in this document or the write-up\".\n\nFiles consulted: /home/HTJ/t2/conf/results/review_next/NEXT_EXPERIMENTS_P3.md, /home/HTJ/t2/conf/results/review_next/NEXT_EXPERIMENTS.md, /home/HTJ/t2/conf/results/review_next/p3_loop_groundwork.txt, /home/HTJ/t2/conf/code/diag_p3_loop.py, /home/HTJ/t2/conf/code/diag_p1_cavity.py, /home/HTJ/t2/conf/code/runner.py, /home/HTJ/t2/conf/code/analysis.py, /home/HTJ/t2/conf/code/pair_tags.py, /home/HTJ/t2/conf/code/arms.py, /home/HTJ/t2/conf/code/common.py, /home/HTJ/t2/Demo/t2_route_a.py, /home/HTJ/t2/Demo/exp_0921_analysis.py, /home/HTJ/t2/conf/01_RULES.md §5, /home/HTJ/t2/conf/08_SPEC_analysis.md §2, /home/HTJ/t2/conf/10_SPEC_stageC.md §3c. MDD script: /tmp/claude-1005/-home-HTJ-t2-Demo/d3790660-21e8-42b4-971e-49b4049a6e64/scratchpad/mdd.py. No repo file was modified."}