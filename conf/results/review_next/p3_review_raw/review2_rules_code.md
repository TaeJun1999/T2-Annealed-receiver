{"message": "Adversarial review of /home/HTJ/t2/conf/results/review_next/NEXT_EXPERIMENTS_P3.md (v1) against HEAD 6bbd487d. Read-only; CPU-only probes run on the real B16e4k build (trials 3200–3202 of the C2 −3 dB stream) from the scratchpad. Defects first, then what was verified as correct.\n\n## Defects (quote → defect → consequence → fix; severity)\n\n1. **§2.3 \"S-conf = alphaD_32 가 가장 큰 실행 (보고 전용, 2차)\"** — the rule is inverted. alphaD = mean(v/tauL) (Demo/t2_route_a.py:418-419) is LARGE on failed blocks and clipped to eps on decoded ones: on the dev set (p1_cavity C2 −3 dB npz) median alphaD@16 = 0.383 (fail) vs 1e-06 (decoded) for V1, 0.374 vs 1e-06 for b*; the doc's own §0 AUC 0.99 means \"larger on failures\". Consequence: S-conf picks the least-confident run, i.e. the one most likely to have failed; the pre-registered report row would show observable selection as harmful for a sign error. Fix: \"alphaD_32 가 가장 **작은** 실행 (동률이면 기본)\"; ties will be common because decoded blocks sit at eps. **High.**\n\n2. **§1 \"`runner.py run --skip0 3200 --n 1280`\"** — the documented launch cannot produce the registered run at HEAD. runner.py:1100-1113 refuses `--skip0 ≠ 0`, `--iters ≠ 16`, `--arm`, `--stagec-ckpt`, `--ntrain ≠ 10000` without `--tag`; with a tag, `A.D2_FITS = gmm_fits_D2_<tag>` (runner.py:109) must exist — every review_next tag has a symlink to `gmm_fits_D2_B16e4k` (`ls conf/results`), which the doc never mentions, so `cmd_run`'s fail-early `load_fits` (runner.py:493-494) raises FileNotFoundError; without `--ntrain 160000` the n160000 fits are not even looked for; without `--stagec-ckpt conf/ckpt/d2sx_N160000_a1.pt` V1 is not built; without `--iters 32` it runs 16. Fix: pin the full command in §1, e.g. `runner.py run --testbed D2 --cell C2 --snr -3 --prior S2 --skip0 3200 --n 1280 --chunk 40 --iters 32 --ntrain 160000 --stagec-ckpt ckpt/d2sx_N160000_a1.pt --arm M-ours-dscore-C-V1 M-ours-bstar R5-genie <p3 arms> --tag review_next_P3` plus the prerequisite `ln -s gmm_fits_D2_B16e4k conf/results/gmm_fits_D2_review_next_P3`, and the same for the two report-only points with `--n 640`. **Medium.**\n\n3. **§5 \"기존 arm 을 `--iters 32` 로 실행하면 … R16 … 비트 동일\"** — true only for the RouteA-family arms. R3-bigamp re-runs the whole block on the beta ladder when divergence is detected at ANY iteration (conf/code/bigamp.py:197-207, `_run_one` diverge check at :240), so a divergence between iterations 17 and 32 changes R3's iterations 1..16 versus a 16-iteration run. Consequence: if the run is launched without `--arm` (the doc's command has none; `--stagec-ckpt` also adds V0/V4/V4b/bstar-scalar), the §5 acceptance check fails for R3 and the cost estimate is off by the extra ~10 arms. Fix: restrict with `--arm` to V1, M-ours-bstar, R5-genie and the four P3 arms, and say the bit-identity claim covers RouteA/RouteAClip only. **Medium.**\n\n4. **§1 \"수용 검사: 점마다 정확히 {(3200+40k, 40) : k=0..31}\" / §1 \"paired 검정 | house test\"** — no existing code implements or even tolerates this set. Verified: `pair_tags.split_of(chunk_plan(3200,1280,40))` → \"DEVELOPMENT … §0 acceptance FAILED -> this run is INVALID\" (pair_tags.py:126-127), and `pair_tags.load` refuses any raw with `run|iters != 16` (pair_tags.py:175-176); `analysis.load_raw`'s hole check starts at DEV_SKIP0=2560 (analysis.py:129) → spurious \"holes/overlaps\" WARNING for a skip-3200 set (verified ok=False); `run_manifest.py:93` labels every run with min(skip) ≥ 2560 as \"DEVELOPMENT set\" → the P3 judging set would be recorded in its manifest as the development set the doc says it is separate from; `runner.py analysis --tag …` (which cmd_run prints as the next step) would read column −1 of a 32-column raw and label it \"@16\"/\"BLER@16\" (analysis.py:206-263). Consequence: the acceptance path and provenance labels are missing or wrong, and a mislabeled tables file can be produced by following the runner's own hint. Fix: add `P3_SKIP0 = 3200` to common.py; make load_raw's start `chunks[0][0] if chunks[0][0] in (0, DEV_SKIP0, P3_SKIP0) else 0`; run_manifest label \"P3 judging set (trials >= 3200)\"; have analysis.main refuse or relabel when `run|iters != N_ITER`; put the 32×40 acceptance check into p3_rules.py (§5 row 4) and state that pair_tags is not used. **Medium.**\n\n5. **§2.2 \"beta = 0.5, 01_RULES §4 의 폴백 방향\"** — misattribution: the §4 β ladder 0.7→0.5→0.3 is BiG-AMP's knob (01_RULES §4 row \"BiG-AMP damping β\"; runner.py:318 comment; `--beta` rebuilds only R3-bigamp, runner.py:318-320). Consequence: an implementer using `--beta 0.5` gets unchanged RouteA arms while `run|beta = 0.5` is written into the raw header. Fix: drop the §4 citation, state \"v1 loop constant `arms.LOOP['beta']`, changed only through `route_a(..., beta=0.5)`; the runner `--beta` flag must stay 0.7\". **Medium-low.**\n\n6. **§2.2 \"D-15/Q-24 는 '감쇠 없음'으로 닫혔으나 여기서는 새 arm 의 ablation 이지 그 결정의 변경이 아니다\"** — legitimacy holds (no headline arm changes; new names; 03_SPEC §1.1 untouched), but the doc omits that Q-24 was closed on a measurement: `beta_fb=0.7` was harmful on exp_0921 A (0:7, 5:24, 5:21, 3:12, 2:16, p ≤ .035; docs/logs/decision_log.md:171, open_questions.md:36, Gaussian Kronecker 4×4, 16 iters). Consequence: §6's H7 prediction \"판정 불가(p ≥ 0.05)\" is written as if no prior evidence existed, and the choice of 0.5 rather than the already-tested 0.7 is unexplained. Fix: cite the Q-24 numbers in §2.2, state that fb05 is a D2 replication at a different value, and predict accordingly (or justify the \"판정 불가\" prediction explicitly). **Low.**\n\n7. **§1 \"관측량 (규칙에 쓸 수 있는 것) | r_t …, alphaD_t, tauL_gmean_t, nu_q_t\"** — `nu_q` is not in `KEYS_RAW` (common.py:78) so it is never saved to raw, and it is NaN for M-ours-bstar (mode='colored', t2_route_a.py:377; verified nu_q@1 = nan). Consequence: it cannot satisfy the §1 symmetry requirement (\"V1 과 GMM b* 에 같은 코드\"). Fix: remove nu_q from the usable list, or add it to the P3 extra-log with an explicit \"V1-only, report-only\" tag. **Low.**\n\n8. **§3 \"H7 | … 어느 변형이든 p < 0.05 로 실패가 적으면 H7 기각\"** — four tests (2 variants × 2 priors) with rejection on any one, no multiplicity statement; family-wise false rejection ≈ 0.19 under the null, and \"기각되면 그 변형이 P3-C 의 재시작 후보\" feeds it forward. Fix: declare H7 per (prior, variant) as four separate secondary verdicts, or use α/4 for the \"any\" rule. **Low.**\n\n9. **§2.3 \"S-res = 32회째 r_32 가 가장 작은 실행\"** — compares state residuals across runs with different loop constants; r_t magnitude depends on the damping constant, not only on convergence (my 3-trial probe showed no systematic direction: r_32 base/b05/fb05 = 1e-13/3.6e-2/5e-8, 4.9e-4/3.9e-4/8.6e-4, 6.9e-2/5.1e-3/1.3e-2 …). Consequence: an H8 outcome is hard to attribute to \"picking the better run\" vs \"picking the more damped run\". Fix: pre-register the selection-frequency table per run as a mandatory report, and add the oracle/S-res restricted to {기본} vs {b05} and {기본} vs {fb05} separately. **Low.**\n\n10. **§3 \"발산 블록(guardH)은 그대로 세고 빼지 않는다\"** — with `--iters 32` the raw `<arm>|guardH` is \"NMSE > 10 at any of the 32 iterations\" (runner.py:466-470; its comment still says 16). Consequence: if p3_rules reports divergence counts per rule, R16's count is inflated relative to a real 16-iteration run. Fix: recompute per-rule divergence from `nmse[:, :t]` (nmse is in KEYS_RAW). **Low.**\n\n11. **§5 row 1 \"`build_our_arms(p3=True)` … 새 이름으로 추가\"** — feasible, but runner.budget_meta/learned only know the existing names (runner.py:233-235, 364-395): the new arms would get \"unclassified arm\" budget strings and no `fit_sec|`/checkpoint identity, which 10_SPEC A3 requires to be readable per arm. Fix: replicate the `mean_arms` wiring (runner.py:386-395) for the P3 names and add the GMM ones to GMM_BUDGET_ARMS. **Low.**\n\n12. **§2.1 \"R16 … 같은 시행의 기존 16 반복 실행과 비트 동일해야 한다\"** — no 16-iteration run exists for trials ≥ 3200 (max skip in any raw dir is 3160 → trials < 3200). §5 row 3 already says a new 16-iteration reference run is needed; it is absent from §4's cost table (small: ~1.1 s + 0.8 s per block). Fix: reword §2.1 to \"새 16 반복 기준 실행\" and add it to §4. **Low.**\n\n## Verified as correct (no defect)\n\n- (1) Offline == online holds for this receiver: `RouteA.run` (t2_route_a.py:339-435) has no schedule keyed to n_iter (`init_colored=0`, `nu_sw=None`), no final-iteration special case, constant `self.beta`; RouteAClip/GMMPriorB counters and `ScorePrior._cache` (keyed on `(q.tobytes(), nu)`, score.py:1111) do not feed back. Empirically, for V1 and M-ours-bstar on trials 3200–3201, iterations 1..16 of a 32-iteration run are bit-identical to a 16-iteration run for every KEYS_LOG field, and repeated 32-runs are bit-identical (no RNG consumed inside run).\n- (3) `blk_err`, `alphaD`, `tauL_gmean` are appended every iteration (t2_route_a.py:432-434) and saved as (n, 32) via KEYS_RAW (runner.py:445). r_t through the diag_p1_cavity Tap/post path works at 32 iterations for both `_matrix_site` (V1) and `ep_site` (b*): 32 hook calls, hpost reproduces the logged NMSE exactly (max rel dev 0), hooked vs unhooked logs bit-identical. diag_p1_cavity.py itself cannot be reused (main() hard-refuses non-DEV chunks, :528-529); a new wrapper is needed, as the doc says.\n- (4) `route_a(**over)` delivers `beta`/`beta_fb` to RouteA and RouteAClip (cfg_dump verified: beta=0.5 / beta_fb=0.5). beta damps only the extrinsic pair (rD, tauD) → the L_X detector prior (t2_route_a.py:427); beta_fb damps (xbar_grid, v_grid) → the L_H input under feedback='posterior' (:429-430). Both live in loop code shared by the score and colored paths, and both variants change both arms' trajectories (max |ΔNMSE| 0.002–0.11 on the probe trials).\n- (5) Equal receiver budget (32 for both priors), CPU complex128 enforced (runner.py:1086-1091), n = 1280/640 ≥ 640, trials ≥ 3200 untouched by every existing raw dir, frozen-config guard requires `--tag` for `--iters 32`/`--skip0 3200` (runner.py:1100-1113).\n- (6) Cost: single-thread 32-iteration block = 2.18 s (V1) / 1.52 s (b*), so a 40-trial chunk with 3 configs × 2 arms + genie ≈ 7.5 min; 32 (+16+16) tasks on ≤ 64 workers → ≈ 10 min wall plus build; §4's \"10~20분\" is consistent, and the quoted bench numbers (V1 1006 ms, b* 754 ms @16) match conf/results/review_next/complexity_moduleH_ep.txt:13-14.\n- §0 numbers match p3_loop_groundwork.txt (88/132 failures, class failure counts 14/1/13/60 and 21/3/21/87, rescued/lost 11/0 and 13/1, AUCs).\n\nScratch scripts (not part of the repo): /tmp/claude-1005/-home-HTJ-t2-Demo/d3790660-21e8-42b4-971e-49b4049a6e64/scratchpad/p3_check.py, p3_check2.py."}