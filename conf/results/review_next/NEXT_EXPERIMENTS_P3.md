# NEXT_EXPERIMENTS_P3 — 반복 루프 개선(P3)의 사전 등록 (v2)

- 작성: 2026-09-22 23:40 CDT 초안(v1) → 적대적 검토 2건(통계·누출 12건, 규칙·코드 12건; `p3_review_raw/`) 반영 v2, 2026-09-23 00:10 CDT (= 09-23 14:10 KST), Claude Code (Fable 5.1). **결과 관측 전.** 커밋 뒤에는 §1~§3·§6 의 규칙·임계값·예측을 바꾸지 않는다. 바꿔야 하면 사용자 승인 + `DECISIONS.md`.
- 근거: `p3_loop_groundwork.txt` (개발 집합 2560..3199 의 반복 궤적 재분석, 새 실행 없음), `REVIEW_AUDIT.md` R-10.2, 핸드오프 §9·§10.2. `NEXT_EXPERIMENTS.md`(v3) §0 의 고정 사항(테스트 집합 0..2559, house test, 평가 가중치, 결과 보존, CPU 수신기)은 그대로 적용된다.
- 목적: **prior 표현력과 분리해서** 반복 루프 자체(반복 수·감쇠·재시작)가 실패를 줄이는지, 그 효과가 GMM 과 diffusion 에 대칭인지 답한다. 루프 개선으로 얻은 이득을 prior 의 이득이라 부르지 않는다.

---

## 0. 기초 자료 (설계의 출발점; 이 시행들은 판정에 쓰지 않는다)

`p3_loop_groundwork.txt`, C2 −3 dB, 개발 집합 2560..3199 (n=640), 반복 13~16 의 상태 잔차 r_t = ‖h_post^t − h_post^{t−1}‖/‖h_post^t‖:

| arm | 실패@16 | settled (max r_13..16 < 1e-3) | 2-cycle | slow (< 1e-2) | moving | 8→16 rescued / lost |
|---|---|---|---|---|---|---|
| V1 (`M-ours-dscore-C-V1`) | 88 | 14 | 1 | 13 | **60** | 11 / 0 |
| GMM b\* (`M-ours-bstar`) | 132 | 21 | 3 | 21 | **87** | 13 / 1 |

실패 블록의 2/3 이상은 16회째에도 **움직이는 중**이다(고정점 아님). 수신기가 볼 수 있는 값이 실패 블록을 거의 완벽히 가른다: AUC r_16 0.98 (V1) / 1.00 (b\*), alphaD@16 0.99 / 0.99, tauL_gmean@16 0.98 / 0.97 — **alphaD 는 실패 블록에서 크다** (alphaD = mean(v/tauL), 검출기 사후 분산비; 풀린 블록은 eps 근처, 개발 집합 중앙값 V1 실패 0.383 vs 성공 1e-6). 단일 반복 기준 r_t < 1e-3 은 일시적으로도 떨어진다: 반복 13~15 는 ≥ 1e-3 인데 16 에서만 < 1e-3 인 블록이 V1 19(실패 4)·b\* 11(실패 6) — §2.1 의 2회 연속 조건은 여기서 나왔다.

## 1. 고정되는 것

| 항목 | 값 |
|---|---|
| **판정 집합 (신규)** | 같은 시행 스트림의 **skip 3200 ~ 4479 (n=1280, 청크 40 × 32)**. 기존 테스트(0..2559)·개발(2560..3199)·기초 자료와 분리(모든 raw 디렉터리의 최대 skip 3160 확인). 수용 검사(`p3_rules.py`): 점마다 raw 청크 집합이 정확히 {(3200+40k, 40) : k=0..31}, 아니면 무효 |
| 판정 점 | **C2 −3 dB 만.** C2 +6 dB·C5 −3 dB 는 보고 전용 (n=640, skip 3200..3839) |
| 구성 | 헤드라인: N_train 1.6e5, GMM b\* kron K=1024 (`gmm_fits_D2_B16e4k`), V1 = `ckpt/d2sx_N160000_a1.pt` (last-EMA, `BEST_WEIGHTS_UNAVAILABLE`; H0 결과 best/last 85:85 로 판정 불가 — A1 이 이긴다면 그 체크포인트로의 재실행은 별도 등록) |
| 수신기 | CPU complex128, 전 arm 동일 시행·파일럿·잡음(paired). 기본 arm 은 `arms.LOOP` (beta 0.7 = **RouteA 의 v1 루프 상수**, beta_fb None, n_inner 1, feedback posterior, loo) 그대로. P3 변형은 **새 arm 이름**으로만 추가되고 기존 arm·헤드라인 표는 무변경. runner 의 `--beta` 플래그(R3-BiG-AMP 전용)는 0.7 로 둔다 |
| 실행 (전부 새 태그) | ① **기준 16 반복 실행** `review_next_P3ref` (V1, b\*, R5-genie; `--iters 16`), ② **32 반복 실행** `review_next_P3` (V1, b\*, `V1-b05`, `bstar-b05`, `V1-fb05`, `bstar-fb05`; `--iters 32`; genie 없음). 두 실행 모두 `--skip0 3200 --n 1280 --chunk 40 --ntrain 160000 --stagec-ckpt conf/ckpt/d2sx_N160000_a1.pt --arm …` 과 `ln -s gmm_fits_D2_B16e4k results/gmm_fits_D2_<tag>` (전체 명령은 §5). 보고 전용 2점은 `--n 640` |
| 실패 지표 | 반복 t 의 `blk_err[:, t−1]` (수신기가 매 반복 기록; 평가에만 참 비트 사용). 발산 판정은 규칙마다 `nmse[:, :t]` 로 다시 계산한다(raw 의 `guardH` 는 32 반복 전체 기준이라 그대로 쓰지 않음) |
| 관측량 (규칙에 쓸 수 있는 것) | **r_t** (상태 잔차; `conf/code` 래퍼가 h_post^t 로 계산, V1·b\* 같은 코드), **alphaD_t**, tauL_gmean_t. 참 H·참 비트·BER·NMSE 는 규칙에 쓰지 않는다(CRC 없음 — 가정하지 않는다). nu_q 는 b\*(colored 경로)에 없으므로 규칙에 쓰지 않는다 |
| 비정상 값 | r_t 가 비유한이거나 ‖h^t‖ = 0 이면 r_t = +∞ 로 다룬다(멈추지 않음; S-res 에서 최댓값). 어떤 실행이 반복 17 이후에 예외로 실패(raised)하면 그 블록의 R16 은 **기준 16 반복 실행**의 값, R32·R-adapt 는 실패, 비용 32 |
| paired 검정 | house test: 같은 시행의 실패 지표 쌍 → 불일치 쌍의 exact 양측 부호검정(`exp_0921_analysis.sign_p`), p < 0.05; 불일치 < 6 → UNDECIDED. **판정은 p 로만**, MDD 는 보고(n=1280, 불일치율 3~8%: 14~22 순 블록). 결과는 항상 셋 중 하나: **지지**(p < 0.05, 실패 적음) / **판정 불가**(p ≥ 0.05, n_d ≥ 6) / **UNDECIDED**(n_d < 6). p ≥ 0.05 를 "효과 없음" 으로 쓰지 않는다 |
| 판정 단위 | **모든 가설은 prior(V1, b\*)별로 따로 판정**한다. 어떤 규칙을 "채택" 하려면 **두 prior 모두**에서 지지돼야 한다(§1 대칭성). 검정 수: H6a 2, H6b 2, H7 4, H8 6, §2.4 상호작용 3 = **17개**, 모두 α = 0.05, 보정 없음, 사후 추가 없음 |
| 보고 전용 (판정으로 승격 금지) | S-conf, 오라클, 변형별 R32 수치(H8 의 비교 대상으로만 쓴다), 선택 빈도표, 비용, 보고 전용 2점 |
| 비용 | 반복 수(평균·최대)와 실경로 블록 시간(`complexity_moduleH_ep.txt`: V1 1006 ms, b\* 754 ms / 16 반복 블록, C2 −3 dB)으로 환산해 함께 보고 |

## 2. 설계

루프는 규칙과 무관하게 진행하므로(규칙은 **언제 멈출지·어느 실행을 고를지**만 정한다) 32 반복을 돌려 두고 규칙을 오프라인으로 적용한 결과는 온라인과 **동일**하다 — RouteA.run 은 n_iter 에 의존하는 일정이 없고 RNG 를 쓰지 않으며(검토에서 시행 3200~3201 로 비트 동일 확인), 이 등가성은 RouteA/RouteAClip arm 에만 해당한다(R3-BiG-AMP 는 발산 시 재실행하므로 P3 에 넣지 않는다).

### 2.1 P3-A 반복 연장 (rule = 언제 멈추나)

- 규칙 (블록마다, 오프라인):
  - **R16** = 반복 16 의 결과. 32 반복 실행의 반복 1~16 로그는 기준 16 반복 실행과 **비트 동일**해야 한다(수용 검사; 어긋나면 실행 무효).
  - **R32** = 반복 32 의 결과.
  - **R-adapt** = t ∈ [16, 32] 중 처음으로 **r_{t−1} < 1e-3 이고 r_t < 1e-3** 인 t 에서 멈춤(없으면 32). 비용 = 멈춘 t.
- **H6a**: R32 는 R16 보다 실패가 적다 (prior 별 부호검정 2개).
- **H6b**: R-adapt 는 R16 보다 실패가 적다 (prior 별 2개). R-adapt 와 R32 의 순 차이, 평균 반복 수(prior 별 1280 블록 평균)는 **보고**.

### 2.2 P3-B 감쇠 (rule = 루프 상수)

- 변형 arm (`build_our_arms(p3_arms=True)`, `route_a(..., beta=0.5)` / `route_a(..., beta_fb=0.5)`):
  - `V1-b05`, `bstar-b05`: beta = 0.5 (extrinsic 쌍 (rD, tauD) 의 감쇠; v1 값 0.7).
  - `V1-fb05`, `bstar-fb05`: beta_fb = 0.5 (사후 피드백 (xbar, v) 의 감쇠). D-15/Q-24 는 exp_0921 A(D1 계열 Gaussian Kronecker 4×4, 16 반복)에서 beta_fb = **0.7** 이 해로웠다는 측정(0:7, 5:24, 5:21, 3:12, 2:16, p ≤ 0.035; `docs/logs/decision_log.md:171`)으로 "감쇠 없음" 을 닫았다. 여기서는 **D2·다른 값(0.5)에서의 재현**이고 새 arm 의 ablation 이지 그 결정의 변경이 아니다.
- **H7-b05, H7-fb05**: 변형의 R16 이 기본 R16 보다 실패가 적다 (변형 × prior = 4개 부호검정, 각각 따로 판정).

### 2.3 P3-C 관측 기준 재시작 (rule = 어느 실행을 고르나)

- 실행 추가 없음: 블록마다 {기본, b05, fb05} 세 실행의 반복 32 결과 중 하나를 고른다.
- **S-res** = r_32 가 가장 작은 실행 (동률·전부 +∞ 이면 기본). **S-conf** = alphaD_32 가 가장 **작은** 실행 (보고 전용; 동률이면 기본).
- **오라클** = 세 실행 중 하나라도 반복 32 에서 성공이면 성공 (참 비트 사용) — 배치 불가 상한 참조, arm 아님.
- **H8**: S-res 는 **기본 R32, b05 R32, fb05 R32 각각**보다 실패가 적다 (prior 별 3개 = 6개 부호검정). 어느 하나라도 지지가 아니면 그 prior 에서 H8 은 지지가 아니다(단일 실행 하나를 고른 것과 구별할 수 없으므로). 선택 빈도표(실행별 선택 수, 실패·성공 블록 나눠서)는 필수 보고. 비용 = 실행 3개.

### 2.4 prior 이득의 분리 (사전 고정된 검정)

- 규칙 ∈ {R32, R-adapt, S-res} 마다, 시행 i 에서 **d_i = (fail_V1^rule − fail_V1^R16) − (fail_b\*^rule − fail_b\*^R16)**, d = 0 제외, exact 양측 부호검정(불일치 < 6 → UNDECIDED). 3개 검정.
- 판정 문장: p < 0.05 이고 #d>0 > #d<0 (b\* 가 더 많이 회복) → "**이득의 일부는 루프 정체였다**"; p < 0.05 이고 반대 → "**루프와 무관한 prior 이득**"; 그 외 → "**판정 불가**". 규칙별 V1 대 b\* 격차의 부호검정 결과는 함께 보고하되 이 판정을 대신하지 않는다.

## 3. 판정·채택·확증 (여기서 고정)

| 가설 | 검정 (C2 −3 dB, n=1280, 같은 시행, prior 별) | 결과 |
|---|---|---|
| H6a | R32 vs R16 | 지지 / 판정 불가 / UNDECIDED |
| H6b | R-adapt vs R16 | 〃 |
| H7-b05, H7-fb05 | 변형 R16 vs 기본 R16 | 〃 (변형·prior 별 4개) |
| H8 | S-res vs {기본, b05, fb05} 의 R32 각각 | 3개 모두 지지일 때만 그 prior 에서 지지 |
| §2.4 | 상호작용 부호검정 (R32, R-adapt, S-res) | 루프 정체 일부 / prior 이득 / 판정 불가 |

- **채택 규칙(테스트 확증 후보)**: H6b 가 **두 prior 모두** 지지이고 두 prior 모두 net(R-adapt − R32) ≤ +3 이면 **R-adapt**; 아니면 H6a 가 두 prior 모두 지지이면 **R32**; 아니면 **후보 없음**. 감쇠 변형·S-res 는 이 문서에서 확증 후보가 되지 않는다(지지되면 별도 등록).
- **헤드라인 교체 없음.** P3 는 "루프 규칙을 바꾼 별도 표" 다. 16 반복 헤드라인 표는 그대로 둔다.
- **테스트 집합 확증(실행 여부만 사용자 결정, 내용은 여기서 고정)**: 1차 비교 = **채택 규칙 vs R16**, V1 과 b\* 각각, 테스트 집합 0..2559 의 **C2 −3/0/+3 dB** (3점 규칙 + 08_SPEC §2 power guard: 3점 모두 있고 ≥ 2점에서 불일치 ≥ 6; 통과 = ≥ 2점에서 채택 규칙의 실패가 적고 p < 0.05), 나머지 SNR 보고 전용. 실행 = 새 태그 32 반복 1회(`--iters 32`, 같은 arm 구성); R16 참조 = 그 실행의 반복 16 결과이며 **`raw_B16e4k` 와 비트 동일**해야 한다(Demo 해시 동일). 통과·실패 어느 쪽이든 별도 표로 기록하고 헤드라인은 바꾸지 않는다.
- 발산 블록은 그대로 세고 빼지 않는다.

## 4. 비용 (같이 보고)

| 실행 | 반복 | 상대 비용 (16 반복 단일 실행 = 1×) |
|---|---|---|
| 기준 실행 P3ref (V1, b\*, genie) | 16 | 1× (+ genie) |
| P3 (기본 V1, b\*) | 32 | 2× |
| P3 (b05, fb05 × 2 prior) | 32 | 4× |
| R-adapt | 멈춘 t | 보고 (평균 t / 16) |
| S-res / S-conf | 32 × 3 실행 | 6× |
| 판정 실행 전체 (C2 −3 dB n=1280 + 보고 전용 2점 n=640) | | 단일 스레드 32 반복 블록 ≈ 2.2 s (V1) / 1.5 s (b\*) 실측 → 32~64 워커로 약 15~25분 (추정) |

## 5. 선행 작업과 실행 현황 (갱신한다)

| 항목 | 상태 |
|---|---|
| `common.P3_SKIP0 = 3200`; `analysis.load_raw` 구멍 검사 시작점 ∈ {0, DEV_SKIP0, P3_SKIP0}; `run_manifest.py` 분할 표기 "P3 judging set (trials ≥ 3200)"; `runner.py analysis` 는 `run\|iters ≠ 16` 인 raw 를 거부(표 이름이 @16 이므로) | **구현** (f5856204; `analysis.iters_guard`, 32회 smoke raw 로 거부 확인; 기존 raw 디렉터리 구멍 검사 결과 불변) |
| `build_our_arms(p3_arms=True)` + runner `--p3-arms` (opt-in, `--tag` 필수): 4 변형 arm, budget_meta/learned 배선(`mean_arms` 와 같은 방식, GMM_BUDGET_ARMS 포함) | **구현** (f5856204; b\* 변형마다 별도 `.view("eta")`, 변형 설정은 raw `meta\|p3_cfg\|<arm>` 에 기록; 요청 arm 미생성 시 runner 오류) |
| r_t 래퍼: RouteA/RouteAClip 인스턴스 훅(`_sites`→b, `_matrix_site`→P,η / `prior.ep_site`→Λ,η)으로 h_post^t 를 받아 r_t 를 계산, 로그에 `r_t` 추가; runner opt-in `--extra-log r_t` 로 raw 저장; 훅 전후 로그 비트 동일 검사(`diag_p1_cavity.py` 의 검사 재사용; 그 스크립트 자체는 DEV 청크만 받으므로 새 래퍼 필요) | **구현·검증** (f5856204, `conf/code/rt_tap.py`; selftest 개발 시행 2560~2561 × 6 arm: 훅 전후 모든 로그 키 비트 동일, 재구성 h_post 의 NMSE 가 로그와 상대오차 0, 32회 실행의 반복 1~16 = 16회 실행 비트 동일; `conf/logs/review_next/rt_tap_selftest.log`) |
| `p3_rules.py`: 수용 검사(32×40 from 3200; P3ref 와 P3 의 반복 1~16 비트 동일), R16/R32/R-adapt/S-res/S-conf/오라클, 규칙별 발산 재계산, prior 별 부호검정 17개, §2.4 상호작용, 선택 빈도표, 평균 반복 수·비용 환산. `pair_tags.py` 는 쓰지 않는다(DEV 청크·iters 16 고정) | **구현** (f5856204; `--selftest` + 개발 시행 4개 종단 smoke(수용 검사 통과, 음성 검사 4종 검출); 판정 데이터 적용 전 적대적 코드 검토) |
| 판정 실행 명령 | `ln -sfn gmm_fits_D2_B16e4k conf/results/gmm_fits_D2_review_next_P3ref` (P3 도 동일) → `runner.py run --testbed D2 --cell C2 --snr -3 --prior S2 --skip0 3200 --n 1280 --chunk 40 --iters 16 --ntrain 160000 --stagec-ckpt conf/ckpt/d2sx_N160000_a1.pt --arm M-ours-dscore-C-V1 M-ours-bstar R5-genie --tag review_next_P3ref` / 같은 인자에 `--iters 32 --p3-arms --extra-log r_t --arm M-ours-dscore-C-V1 M-ours-bstar V1-b05 bstar-b05 V1-fb05 bstar-fb05 --tag review_next_P3`; 보고 전용 2점은 `--cell C2 --snr 6` / `--cell C5 --snr -3` 에 `--n 640` |
| 테스트 집합 확증 (§3, 사용자 결정 10:15 CDT) | **완료 — V1·b\* 모두 PASS** (실행 `run_p3test.sh` 10:12~11:02 CDT @d00bb877, 판정 `p3_rules.py --confirm` @2b599c14; R16 = raw_B16e4k 비트 동일, Demo 해시 동일). 수치는 §7.4 | `p3_confirm_C2.txt`, `run_manifest_review_next_P3test.json` |
| 판정 실행 | **완료** (실행 09:35~09:46 CDT, 코드 f5856204; 판정 `p3_rules.py` 09:58 CDT, 코드 39e13126). 세 점 모두 수용 검사 통과. 판정점 C2 −3 dB: **H6a·H6b 두 prior 모두 지지 → 채택 규칙 R-adapt** (테스트 확증 후보; 실행 여부는 사용자 결정), H7 지지 없음(b\* 는 b05·fb05 모두 반대 방향 유의), **H8 두 prior 모두 지지**(두 해석 동일), §2.4 세 규칙 모두 판정 불가. 수치는 §7 | `p3_rules_{C2_m3,C2_p6,C5_m3}.{txt,npz}`, `run_manifest_review_next_P3{,ref}.json` |

### 5.1 구현 시 해석 명확화 (2026-09-23 09:57 CDT = 23:57 KST, 커밋 39e13126 09:58 CDT; 판정 데이터를 열기 전(p3_rules 판정 실행 09:58:43 CDT); §1~§3 은 바꾸지 않는다)

p3_rules.py 를 판정 데이터에 적용하기 전의 적대적 코드 검토(4 lens + 검증)에서 문구가 두 가지로 읽히거나 비어 있는 곳이 나왔다. 아래를 **1차 해석**으로 고정한다. 다른 해석의 값도 함께 출력하고, 판정이 해석에 따라 갈리면 `READING-DEPENDENT` 로 표시한다(`NEXT_EXPERIMENTS.md` §6.1 선례). `conf/DECISIONS.md` 같은 날 항목.

1. **S-res·S-conf 의 동률** "(동률·전부 +∞ 이면 기본)": 1차 = **가장 작은 실행; 최솟값 동률 안에서는 기본 우선, 그다음 b05, fb05; 전부 +∞ 이면 기본**. 본 조항("가장 작은 실행")과 §1("+∞ … S-res 에서 최댓값")에 모두 맞는 읽기다. 문자 해석(최솟값에 동률이 있으면 기본이 최솟값이 아니어도 기본)은 `S-res|literal` 로 병기하고, H8·§2.4(S-res)를 두 해석으로 모두 계산한다. b05 = fb05 < 기본인 블록 수와, 그중 b05·fb05 결과가 다른 블록 수(= b05 우선 순서에만 달린 블록)를 보고한다. 최초 구현(f5856204)은 문자 해석이었다.
2. **감쇠 변형 arm 의 예외(raised)**: 기준 16회 실행(①)에 변형이 없어 §1 "비정상 값" 의 R16 대체를 적용할 수 없다(공백). 변형의 R16 은 실패로 세고, 변형별 예외 수와 그 시행을 뺀 H7 민감도 줄을 함께 출력한다(판정은 바꾸지 않는다).
3. **판정 집합 노출 기록**: 동결 전 검토(`p3_review_raw/review2_rules_code.md`)는 판정점 C2 −3 dB 의 시행 3200~3202 에서 V1·b\*·b05·fb05 의 r_32 와 ΔNMSE 를 관측했다(실패 여부는 보지 않음). §2 에는 3200~3201(비트 동일 검사)만 적혀 있다. 이 시행들은 판정에서 빼지 않는다.
4. 발산 수는 S-res·S-conf(선택된 실행 기준)에도 규칙별로 출력한다. 선택 빈도표는 각 규칙이 고른 실행의 반복 32 결과로 나눈다.

## 6. 미리 적는 예측

- H6a: V1·b\* 모두 지지(p < 0.05). 근거: 8→16 에서 rescued 11/13, lost 0/1 → n=1280 이면 순 15~25 블록.
- H6b: V1·b\* 모두 지지; R-adapt 와 R32 의 순 차이 ≤ +3, 평균 반복 ≈ 18~20 → 채택 규칙 = R-adapt.
- H7-b05: 판정 불가. H7-fb05: 기본보다 실패가 **더 많음**(Q-24 의 0.7 결과 방향) 또는 판정 불가.
- H8: 세 비교 중 하나 이상이 판정 불가(선택이 대부분 기본을 고름) → 지지 아님. 오라클은 실패를 30% 이상 줄이지만 배치 불가.
- §2.4: 세 규칙 모두 판정 불가(두 prior 가 비슷하게 회복), 규칙별 V1 < b\* 격차는 유지(p < 0.05).
- 빗나가면 그대로 쓴다.

## 7. 판정 결과 (2026-09-23 09:58 CDT, `conf/code/p3_rules.py` @39e13126; 이 절은 추가만 한다)

실행: `raw_review_next_P3ref`(16회: V1, b\*, genie) / `raw_review_next_P3`(32회 + r_t: V1, b\*, 변형 4), 09:35~09:46 CDT, 코드 f5856204, CPU complex128. 세 점 모두 §1·§2.1 수용 검사 통과(청크 집합, run 파라미터, 체크포인트, b\* kron K=1024, N_train 1.6e5, 변형 루프 상수, r_t, 반복 1~16 비트 동일). 예외(raised) 0, 모든 규칙에서 발산 0(보고 전용 C5 −3 dB 의 bstar-fb05 만 2). 산출물 `results/review_next/p3_rules_<점>.{txt,npz}`.

### 7.1 판정점 C2 −3 dB (n = 1280, 시행 3200..4479)

실패 블록 수 (R5-genie @16: 53):

| prior | R16 | R32 | R-adapt (평균 반복, 최대) | b05 R16 / R32 | fb05 R16 / R32 | S-res | S-conf (보고) | 오라클 (보고) |
|---|---|---|---|---|---|---|---|---|
| V1 | 173 | 154 | 154 (17.80, 32) | 175 / 154 | 183 / 169 | 144 | 139 | 135 |
| b\* | 307 | 290 | 290 (18.75, 32) | 319 / 299 | 326 / 307 | 273 | 265 | 262 |

R-adapt 의 결과는 모든 블록에서 R32 와 같다(블록별 일치; 세 점 모두).

| 가설 | V1 (a:b, p → 결과) | b\* |
|---|---|---|
| H6a R32 vs R16 | 0:19, p = 3.8e-6 → **지지** | 1:18, p = 7.6e-5 → **지지** |
| H6b R-adapt vs R16 | 0:19, p = 3.8e-6 → **지지** | 1:18, p = 7.6e-5 → **지지** |
| H7-b05 변형 R16 vs 기본 R16 | 13:11, p = 0.84 → 판정 불가 | 20:8, p = 0.036 → **반대 방향**(b05 실패가 많음) → 지지 아님 |
| H7-fb05 | 26:16, p = 0.16 → 판정 불가 | 37:18, p = 0.015 → **반대 방향**(fb05 실패가 많음) → 지지 아님 |
| H8 S-res vs 기본 / b05 / fb05 R32 | 2:12 p = 0.013 / 4:14 p = 0.031 / 5:30 p = 2.2e-5 → **지지** | 3:20 p = 4.9e-4 / 3:29 p = 2.6e-6 / 7:41 p = 6.2e-7 → **지지** |

(a = 앞쪽만 실패, b = 뒤쪽만 실패.) §5.1 의 두 동률 해석은 S-res 선택이 세 점 모두 한 블록도 다르지 않아 H8·§2.4 결과가 같다(S-conf 는 판정점에서 2블록 달라 139/141, 265/267). b05 = fb05 < 기본 동률 블록 0.

§2.4 prior 이득 분리 (d = (V1^규칙 − V1^R16) − (b\*^규칙 − b\*^R16)): R32 17:19 p = 0.87, R-adapt 17:19 p = 0.87, S-res 36:30 p = 0.54 → **세 규칙 모두 판정 불가**. 함께 보고하는 V1 대 b\* 격차: R16 24:158 (p = 2.2e-25), R32 18:154 (p = 4.1e-28), R-adapt 동일, S-res 14:143 (p = 4.2e-28).

**§3 채택**: H6b 가 두 prior 모두 지지, net(R-adapt − R32) = V1 0 / b\* 0 (≤ +3) → **채택 규칙 = R-adapt**. 테스트 집합 확증(§3 에 고정된 내용)의 실행 여부는 사용자 결정이다. S-res 는 H8 이 지지됐지만 §3 에 따라 이 문서에서는 확증 후보가 아니다(확증하려면 별도 등록).

비용(보고, `complexity_moduleH_ep.txt` 블록 시간을 반복 수에 비례 환산한 추정): R-adapt 평균 V1 17.80회 ≈ 1119 ms (R16 1006 ms), b\* 18.75회 ≈ 884 ms (754 ms); R32 2012 / 1508 ms; S-res 3 × 32회 = 6036 / 4524 ms.

### 7.2 보고 전용 점 (n = 640, 시행 3200..3839; 판정 아님)

| 점 | prior | R16 | R32 = R-adapt (평균 반복) | S-res | 오라클 | H6a (a:b, p) | H8 (3개 비교) | §2.4 |
|---|---|---|---|---|---|---|---|---|
| C2 +6 dB | V1 | 1 | 1 (16.08) | 1 | 1 | 0:0 UNDECIDED | UNDECIDED | UNDECIDED |
| C2 +6 dB | b\* | 6 | 6 (16.12) | 5 | 5 | 0:0 UNDECIDED | UNDECIDED | |
| C5 −3 dB | V1 | 230 | 209 (21.05) | 185 | 178 | 0:21, 9.5e-7 | 4:28 / 1:21 / 4:41, 모두 p < 1e-4 | R32·R-adapt 15:19 p = 0.61, S-res 36:43 p = 0.50 |
| C5 −3 dB | b\* | 298 | 281 (22.41) | 260 | 254 | 1:18, 7.6e-5 | 1:22 / 1:27 / 5:41, 모두 p < 1e-5 | |

C5 −3 dB 의 H7 은 네 검정 모두 판정 불가(V1 b05 14:18, fb05 28:20; b\* b05 12:10, fb05 32:25). C2 +6 dB genie 1, C5 −3 dB genie 30.

### 7.3 §6 예측 대조 (빗나간 것도 그대로)

| 예측 | 결과 |
|---|---|
| H6a: 두 prior 지지, 순 15~25 | **적중** (순 19 / 17) |
| H6b: 두 prior 지지, net(R-adapt − R32) ≤ +3, 평균 반복 ≈ 18~20 → 채택 R-adapt | **적중** (net 0 / 0, 채택 R-adapt). 평균 반복 V1 17.80 은 예측 범위 하한보다 약간 낮고 b\* 18.75 는 범위 안 |
| H7-b05: 판정 불가 | V1 **적중**, b\* **빗나감** (b05 가 기본보다 실패가 많음, p = 0.036) |
| H7-fb05: 기본보다 실패가 많음 또는 판정 불가 | **적중** (V1 판정 불가, b\* 많음 p = 0.015) |
| H8: 셋 중 하나 이상 판정 불가 → 지지 아님 (선택이 대부분 기본) | **빗나감** — 두 prior 모두 세 비교 전부 지지. 선택 분포: V1 기본 579 / b05 562 / fb05 139, b\* 1006 / 148 / 126 |
| 오라클은 실패를 30% 이상 줄임 | **빗나감** — 기본 R32 대비 V1 −12% (154→135), b\* −10% (290→262); R16 대비 −22% / −15% |
| §2.4: 세 규칙 모두 판정 불가, 규칙별 V1 < b\* 격차 유지 (p < 0.05) | **적중** |

### 7.4 테스트 집합 확증 (§3; 사용자 결정 2026-09-23 10:15 CDT "지금 실행"; 한 번)

실행 `conf/code/run_p3test.sh` (태그 `review_next_P3test`, 테스트 시행 0..2559, C2 7개 SNR, 32회 + r_t, P3 와 같은 arm 구성), 10:12~11:02 CDT, 코드 d00bb877. 판정 `p3_rules.py --confirm` @2b599c14 (테스트 결과를 보기 전에 커밋; 적대적 검토 wf_481ecc8d-2cb 반영). R16 기준 = 이 실행의 반복 16, **raw_B16e4k 와 V1·b\* 의 모든 KEYS_RAW 필드가 7개 SNR 모두 비트 동일**, 두 실행의 Demo 해시 동일. 예외 0, 발산 0. 산출물 `results/review_next/p3_confirm_C2.txt`.

| SNR | V1 R16 → R-adapt (R32) | a:b, p | b\* R16 → R-adapt (R32) | a:b, p |
|---|---|---|---|---|
| −3 dB (판정) | 371 → 342 (341) | 2:31, 1.3e-7 → 지지 | 623 → 589 (589) | 1:35, 1.1e-9 → 지지 |
| 0 dB (판정) | 88 → 79 (79) | 0:9, 0.0039 → 지지 | 184 → 162 (159) | 0:22, 4.8e-7 → 지지 |
| +3 dB (판정) | 29 → 25 (24) | 0:4 → UNDECIDED | 57 → 49 (49) | 0:8, 0.0078 → 지지 |
| +6 / +9 / +12 / +15 dB (보고) | 16→15 / 7→7 / 5→4 / 3→3 | 전부 UNDECIDED | 28→28 / 17→13 / 16→16 / 8→7 | 전부 UNDECIDED |

**3점 규칙: V1 2/3 승 + power guard 충족 → PASS; b\* 3/3 승 → PASS** (pooled, 보고 전용: V1 2:44 p = 3.1e-11, b\* 1:65 p = 1.8e-18). R16 실패율(V1 371/2560 = 0.1449, b\* 623/2560 = 0.2434)은 헤드라인 표(`tables_D2_B16e4k.txt`)의 값과 같다. **헤드라인 표는 바꾸지 않는다**(§3); 이 결과는 "루프 규칙을 R-adapt 로 바꾼 별도 표" 다.

보고 전용: R-adapt 평균 반복 V1 17.90 / 16.55 / 16.20 (−3/0/+3 dB), b\* 18.72 / 16.77 / 16.18. 판정 집합과 달리 테스트 집합에서는 R-adapt 와 R32 가 몇 블록 다르다(V1 −3 dB 342 vs 341, +3 dB 25 vs 24; b\* 0 dB 162 vs 159 — R-adapt 가 멈춘 뒤 R32 에서 풀린 블록). S-res(보고 전용, 확증 후보 아님) V1 303 / 58 / 19, b\* 548 / 131 / 34; 오라클 V1 287 / 55 / 15, b\* 529 / 115 / 30.
