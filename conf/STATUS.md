# STATUS — conf 실험 (Stage A/B 완료 · Stage C 진행 중)

## 세션 설정
- 모델 : **Opus 5 (1M context)** (`claude-opus-5[1m]`). effort/thinking 은 세션 내부에서 확인·변경 불가한
  클라이언트 설정이라 모델이 검증할 수 없음.
- 기간 : 2026-09-20 00:22 KST ~ 2026-09-21 10:10 KST (약 34시간)
- 자원 : NVIDIA RTX PRO 6000 Blackwell x 6 (각 97887 MiB, Exclusive_Process), CPU 192코어.
  수신기 실행 = **CPU 전용 complex128** (전 arm 동일) / score 학습·게이트 = GPU float32.
  -> 테스트 **G1·G2 는 N/A** (수신기에 GPU 를 쓰지 않았으므로).

---

# 완료 : DoD 9항목 전부 충족

| # | DoD 항목 | 상태 |
|---|---|---|
| 1 | 테스트 B·S·C·M·L 전 항목 PASS | **35/35 PASS** (S4 N/A, S5·S7·T2b·T2c·T2e RECORD) |
| 2 | `results/sigma_grid.txt` | 있음 (측정 20점, D2 판 별도) |
| 3 | 학습 증거 | ckpt **38개 1517 MB**, LADDER **82행**, `gate_D1.txt`, train 로그 38개, **ABORTED 0건** |
| 4 | D1 전 arm 표 + 순환 testbed 경고 | `tables_D1.txt` (경고 삽입 확인) |
| 5 | T2a~T2e | `testbed_D2.txt`, **T2d PASS** |
| 6 | `gmm_fit_D2.txt` | 있음 (K 16~512 x full/kron x kappa x restart) |
| 7 | D2 전 arm 표 (headline) | `tables_D2.txt` (claim testbed 경고 삽입 확인) |
| 8 | 표 A/B/C/D, n>=640, headline 2점 n>=2560 | 전부 있음. C1 +6/+9 dB, C2 -3/0 dB 에서 n=2560 |
| 9 | STATUS 최종 + push | 이 파일 |

**셀 9개 x SNR 격자, raw 1056개.** D1: C1~C5 / D2: C1~C5.

---

# 핵심 결과

## 1. 수신기 — D-15 + LOO 루프 (양 testbed 유의)

| 비교 | D2 C2 | p |
|---|---|---|
| R1-turbo -> R2-ours-G | **+2.77 dB** [2.13, 3.42] | 8.1e-97 |
| R2-ours-G -> R4-scvamp (최근접 선행의 Onsager 인터페이스) | R2 가 **+5.61 dB** 우세 | 2.2e-236 |
| R4-llr -> R4-scvamp (원문 ablation 재현) | Onsager 우세 | 1.2e-07 |
| R2-ours-G -> R3-bigamp | R2 압도 | 매우 유의 |

`09_PRIOR_ART_NOTE` 가 지목한 스쿠핑 위험(arXiv:2604.19061)에 대한 직접 답 — **저들의 인터페이스만으로는
우리 설정(미지 bilinear 채널)에서 작동하지 않는다.**

## 2. Module H (구조화 GMM) — **headline**

**D2 C2, n=2560 (결정점):  R2-ours-G -> M-ours-bstar  = +0.49 dB [0.30, 0.72],
3/3 점 전부 유의, pooled 374:126, p=1.4e-29**

- **M2 = 정확히 0.0** 이 이 격차가 **Module H 단독의 몫**임을 증명 (다른 모든 설정 동일)
- b* = kron K=256, **검증 우도만으로 선택** (BLER 미사용). K 격자는 b* 가 경계에 놓일 때마다
  128 -> 256 -> 512 로 확장했다 — GMM 을 강하게 만드는 = **우리 주장에 불리한** 방향

## 3. 이득이 나타나는 조건 (체제 특성)

Tp 2/3/4 x SNR 7점 x 2 testbed 에서 일관된 패턴: **Module H 는 채널 추정이 부정확한 저SNR 에서 이득을
주고, 고SNR 에서는 이득이 사라지거나 역전된다** (D2 C5 +15dB: R2 0.116 vs bstar 0.159, 36:64 p=0.0066).
D2 C1(Tp=2)에서 이득이 없는 것은 T2c 가 사전에 진단한 대로 **첫 패스가 무정보**(NMSE_inf 0.466 ~ 1-Tp/Nt 0.5)
이기 때문이고, 그 셀은 표에서 빼지 않고 진단 결과로 보고한다.

## 4. 학습 diffusion prior — BLOCKED, 그러나 정량화된 음성 결과

**총 959 시도** (사전 등록 사다리 18 + 확장칸 15 + TPE 724 + 균등표본 196 + 최종 재게이트 6) **전부 게이트 미달.**

동일 1e4 예산에서 **GB(0.0117<=0.05) 와 GD(0.161<=0.20) 는 충족, GC 만 0.243 vs 0.15 로 1.62배 부족.**

**표본복잡도 (보고 전용, arm 비교는 1e4 고정):**

| N_train | GB | GC | GD | 판정 |
|---|---|---|---|---|
| 2,500 | 0.0552 | 0.785 | 0.324 | FAIL |
| **10,000** (arm 예산) | 0.0117 | 0.243 | 0.161 | FAIL |
| 40,000 | 0.0045 | 0.167 | 0.101 | FAIL |
| **160,000** | 0.0027 | **0.0999** | 0.0718 | **PASS** |

`GC ~ N^(-0.474)` (R^2 0.934), 0.15 교차점 **N ~ 5.3e4** — 40k/160k 사이로 **측정된** 교차.
-> 결론은 "해봤는데 안 됨"이 아니라 **"동일 예산에서 GC 만 1.62배 부족하고 기준 도달에 ~1e5 필요"** 이다.

부족분이 아키텍처나 인터페이스 탓이 아님도 확인했다:
- 아키텍처: 959시도 + **균등표본 n=30씩 6종** 비교에서 mlp 만 명확히 열등(4.540), 나머지 5종
  (unet 1.572 / uvit 1.814 / dit 1.865 / adm 1.872 / conv 1.902) 은 **통계적으로 구분 불가**
- 인터페이스: 정확 score 를 one-shot D-13+D-14 로 넣은 것과 정확 mixture-EP site 의 차이가
  **유의하지 않음** (p=0.14, -0.07 dB [-0.55, +0.39])

## 5. 이론·검증 앵커

- **F2 lemma** : 복호기 soft output = 부호심볼 prior 의 정확 Tweedie score. `max|tanh-E| = 2.33e-15`,
  Tweedie 유한차분 `6.89e-10`. 복소 규약 `L_c = 4/sigma_c^2` 확인 (`2/sigma_c^2` 는 오차 0.495)
- **T2d** : 조건부 Gaussian 파괴를 **메커니즘까지** 검증. L in {3..8} 전 구간 층화, 측정 vs 해석해
  `2 - sum p^2` 편차 **5.0e-3**, 99.9% CI 전부 Gaussian 값 2 를 배제 (worst CI_hi-2 = -0.2518)
- **M2 = 0.0**, M1 = 0.0 (기존 exp_0925 arm 과 비트 동일)

---

# BLOCKED
- `M-ours-dscore` : 게이트 통과 체크포인트 없음 -> D1·D2 양쪽 표에서 BLOCKED/FAILED-VERIFICATION 표기.
  **게이트를 낮추지 않았다.**

# DECISIONS
- 34건 (`conf/DECISIONS.md`)

# 주요 산출물
`results/` : tables_D1.txt, tables_D2.txt, gate_D1.txt, gate_D2.txt, samplecx_D1.txt, hpo_D1.txt,
hpo_strat_D1.txt, testbed_D2.txt, gmm_fit_D2.txt, lemma.txt, sigma_grid.txt, sigma_grid_D2.txt,
tests.txt, ckpt_inventory.txt / `figs/F2_lemma.png` / `LADDER.md` (82행) / `raw/` (1056)

---

# Stage C — 학습 prior 예산 교정 재시도 (2026-09-21 14:00 KST 개시, 진행 중)

사전 등록: `10_SPEC_stageC.md` (커밋 bce4e62, **결과 관측 전**에 커밋됨).
**Stage A/B 판정은 전부 동결.** `tables_D1/D2.txt`, `gate_D1.txt`, `LADDER.md` 불변이며
`M-ours-dscore` 는 본 표에서 BLOCKED 유지. Stage C 산출물은 전부 별도 파일(`*_C.*`, `LADDER_C.md`).

## C0 — 왜 재시도하는가 (전부 Stage C 착수 이전에 측정된 사실)

`results/samplecx_D1.txt` / `samplecx.csv` — 게이트 vs 훈련 표본 수:

| N_train | GA | GB (≤0.05) | GC (≤0.15) | GD (≤0.20) | gate_score | 판정 |
|---|---|---|---|---|---|---|
| 2,500 | 1.5e-15 | 0.0552 | 0.785 | 0.324 | 5.24 | FAIL |
| **10,000** (등록 예산) | 1.1e-15 | **0.0117 ✅** | 0.243 ❌ | **0.1608 ✅** | 1.62 | FAIL |
| 40,000 | 1.0e-15 | 0.0045 ✅ | 0.167 ❌ | 0.1010 ✅ | 1.11 | FAIL |
| **160,000** | 9.9e-16 | **0.0027 ✅** | **0.0999 ✅** | **0.0718 ✅** | **0.666** | **PASS** |

등록 예산에서 이미 **GA·GB·GD 통과**, 놓친 것은 GC 하나(배율 1.62). GC ~ N^(-0.474) (R² 0.934),
임계 통과가 4e4(FAIL)–1.6e5(PASS) 사이에서 **실측으로** 괄호됨. 즉 등록된 부정 결과는
"방법 부적합"이 아니라 **"예산 부족"**이다.

## C1 — 붕괴 메커니즘 진단 (완료분)

보조 BLER 실행(`tables_D2_supp.txt`)에서 N=1e4 모델은 BLER 0.31~0.90 으로 붕괴했고
**NMSE 가 SNR 상승과 함께 1 을 넘었다**(6 dB 에서 1.11) — 부정확이 아니라 **발산**.

원인 확정 (`results/diag/jacobian-psd_*`), D2 σ 격자 k=6, n=256:

| 모델 | site 공분산 indefinite | belief 평균 이탈 shift(중앙값) |
|---|---|---|
| **diffusion (N=1e4)** | **255 / 256** | **1.87** (p90 59.4, max 1.5e6) |
| GMM-exact (kron K=512) | **0 / 256** | 3.5e-4 (max 9.3e-3) |

수신기 실측 (`jacobian-psd_receiver_clip_C2.txt`, raw_supp 640 trial/SNR):

| arm | 클리핑 비율 | shift 평균 | shift 최대 |
|---|---|---|---|
| **M-ours-dscore** | **0.99 ~ 1.00** | **4.3e3 ~ 1.1e5** | **5.5e7** |
| M-ours-bstar | 0.44 ~ 0.74 | 1.4e-4 ~ 5.5e-3 | 6.5e-2 |
| M-ours-gmm32 | 0.30 ~ 0.57 | 7.1e-5 ~ 3.4e-3 | 3.1e-2 |

**메커니즘**: 참 posterior-mean 디노이저는 J = Cov(h|q)/σ² 이므로 대칭 PSD 가 강제되지만
신경망 야코비안은 그렇지 않다 → D-14 행렬 site 가 부정부호 → LAM_MIN 바닥치기 →
site 의 평균 η 와 정밀도 Λ 가 상호 모순 → belief 평균이 6~8 자릿수 이탈 → 루프 발산.

**이것이 GB′(posterior 평균)와 Hyvarinen(score)이 둘 다 학습 prior 우세를 보고하면서도
BLER 이 붕괴한 이유다. 깨진 것은 평균도 score 도 아니고 오직 미분이다.**

### 정정 — "GD 가 그 양이다" 는 틀렸다 (2026-09-21 15:00, results/diag/SUMMARY.md)

앞서 나는 "실패한 게이트 GD 가 바로 문제의 그 양"이라고 썼다. **오류다.** 실제로:
- 문제의 D2 체크포인트 구성(HPO trial 386)은 D1 재게이트에서 **GD 0.1499 = PASS**,
  **GC 0.2241 = FAIL** 이다 (`results/hpo_final_r2.txt`). 내가 인용한 GD 0.27~0.32 는
  다른 사다리 칸들(L4u_a3 0.3198, L5_a3 0.3859 등)의 값이지 이 구성의 값이 아니다.
  최종 재게이트 6개 중 4개가 GD 를 통과하고 6개 전부가 GC 에서 실패한다.
- 더 중요한 것: **GA~GD 중 어느 것도 PSD 성을 시험하지 않는다.** GD 는 J 의 상대 Frobenius
  오차인데, 15% 의 상대오차는 부호를 가르는 |λmin| ≈ 3e-3 보다 한 자릿수 크다. 즉 GD 를
  통과해도 J 가 부정부호일 수 있고 실제로 그렇다.

**정정된 주장**: 사전 등록 배터리는 **옳은 결론(BLOCKED)에 도달했지만 옳은 메커니즘을 통해서가
아니었다.** 차단한 것은 GC(1차량)이고, 수신기를 부순 것은 J 의 고유값 **부호**다. 빠진 시험
`frac λmin(Herm J) < 0` 은 GA~GD 와 달리 **D2 에서 정답 없이 측정 가능하다.**

### 인과 개입 — 양방향 do() 실험 (n=128, C2, 미게이트 N=1e4 모델, 진단 전용)

| arm | 개입 | BLER@0dB | @6dB | NMSE med@0dB |
|---|---|---|---|---|
| dscore\|eta (= M-ours-dscore) | 없음 | 0.922 | 0.719 | 1.328 |
| **dscore\|psd1e-2** | 고유값 [1e-2,1] 클램프 | **0.031** | **0.000** | 0.024 |
| dscore\|mean | `clip='mean'` 로만 변경 | 0.047 | 0.000 | 0.024 |
| dscore\|abs1e-2 | \|eig\| 바닥, **부호 유지** | 0.922 | 0.578 | 0.560 |
| dscore\|belsc | D-14 site 제거 | 0.047 | 0.016 | 0.024 |
| gmmB\|eta (정확 GMM) | 없음 | 0.109 | 0.031 | 0.052 |
| 학습 평균 + **정확 J** | J 만 교체 | 0.062 | 0.000 | — |
| 정확 GMM + **학습 J** | J 만 주입 | **0.953** | **0.922** | — |

McNemar 짝지음 단측, `g|scorew -> +dJ` 54:0 (0dB) / 57:0 (6dB), 각 p < 1e-13.

읽기: (i) 야코비안만 주입해도 정확 GMM 이 무너지고, 야코비안만 고쳐도 학습 prior 가 살아난다 —
**양방향 인과**. (ii) 부호를 유지한 채 조건수만 고치면 회복이 **없다** → 원인은 ill-conditioning
이 아니라 **부호**. (iii) 붕괴는 "부정부호 J" 와 "`clip='eta'` 규칙" 의 **연접**이며, 두 수리가
각각 독립적으로 충분하다. (iv) 수리된 학습 prior 는 정확 GMM 을 **3.5배** 앞선다 (0.031 vs 0.109).

**경고**: 위는 전부 n=128 진단 탐침이며 **게이트를 통과하지 못한 N=1e4 모델**로 얻었다.
어떤 표에도 arm 으로 넣지 않는다. 확증은 게이트 통과 N=1.6e5 모델로 n≥2560 에서 1회 실행한다.

### 기각된 가설
H2 σ 외삽 (범위 밖 질의 0/192 인데 반복 1 에서 이미 NMSE>1 이 42~83%) · H3 정규화/스케일
(전력비 0.998, vec 순서 차이 0.000e+00) · H5 체크포인트 출처 (sha·wall_sec·10528 키 비트 일치).
H4 배선은 단독 원인으로는 무죄이나 필요 공인자다.

### 남은 공백 (숨기지 않는다)
BLER 의 SNR 비단조성은 **부분적으로만** 설명된다. 12 dB 에서 정규화 비율이 되레 꺾이고
(dscore/bstar 109.7 → 55.8 → 100.0), 6 dB 의 NMSE 정점(1.110)에는 반사실 실험이 없다.
정산 실험이 `code/diag_psd_cf.py` 로 사전 등록되어 있다 (7개 SNR 전부, n≥256, 짝지음).

### 부수 발견 (문서 결함)
`results/gate_D2.txt` 의 GB′ 표는 형제 시드 `ckpt/B3_dscore_D2.pt` 로 계산됐고 평가 대상 a1 이
아니었다. a1 로 직접 재측정: worst_excess = **−0.1227** (기록값 −0.1219). 결론은 움직이지 않는다.

## C2/C3 — 진행 중

| 작업 | 상태 |
|---|---|
| D1 N=1.6e5 게이트 정식 기록 → `gate_D1_C.txt`, `LADDER_C.md` | **PASS** (아래) |
| D2 score N=1.6e5 학습 (dit/vp/angle, HPO 최적 구성 그대로) | 실행 중 (epoch 259, val 3.678e-1) |
| GMM D2 N=1.6e5 재적합 (동일 예산, K≤512, 180 EM) | 실행 중 (~6h 예상) |
| GMM D2 N=4e4 재적합 | 167/180 |

## 미해결 / 다음

- N=1.6e5 모델(GD 0.072)의 야코비안도 부정부호인가? **이것이 Stage C 의 분기점.**
  아니면 예산만으로 해결, 맞으면 사전 등록된 수정 (F1) 대칭 PSD 투영을 적용한다.
- (F1)(F2)(F3) 적용 여부는 `10_SPEC_stageC.md` §3 의 사전 정당화에 따르며 BLER 을 보고 정하지 않는다.


## C3 결과 — D1 게이트 **통과** (2026-09-21 14:12 KST)

`results/gate_D1_C.txt` / `LADDER_C.md`, 체크포인트 `ckpt/sx_N160000_D1.pt`,
구성 `dit vp/angle w64 d6 lr2.238e-3 ema0.999`, `n_eval=512 n_jac=64`, 보고용 stream 10.

| 게이트 | 값 | 임계 | 판정 |
|---|---|---|---|
| GA | 9.87861e-16 | ≤ 1e-6 | PASS |
| GB | 2.65940e-03 | ≤ 0.05 | PASS |
| GC | 9.98591e-02 | ≤ 0.15 | PASS |
| GD | 7.18281e-02 | ≤ 0.20 | PASS |
| **VERDICT** | | | **PASS** |

임계값은 동결된 사전 등록값 그대로이며 `GD_trace 0.0075` 는 보고 전용.
사전 등록 파일(`tables_D1/D2.txt`, `gate_D1.txt`, `LADDER.md`)은 `git status` 로 불변 확인됨.

**이것이 의미하는 것**: 등록 예산 1e4 에서의 실패는 예산 부족이었다는 H_C 의 전반부가 확인됐다.
**의미하지 않는 것**: D1 게이트 통과는 D2 수신기 동작을 보증하지 않는다. 게이트는 D1 에서만
측정 가능하고(참 score 가 D1 에만 있다), 붕괴의 직접 원인인 **D2 에서의 야코비안 유효성**은
별도 측정 중이다 (`jacobian-psd_D2_N*`). 그것이 Stage C 의 실제 분기점이다.

### D2 야코비안 예산 스윕 (진행 중) — N=1e4 기준선

`logs/jacpsd_N10000_a1.log`, D2 σ 격자, n=128/점:

| k | σ | asym(Jr) | lmin(Jr) | f<0 | clipfrac | shift 중앙값 | shift 최대 |
|---|---|---|---|---|---|---|---|
| 0 | 3.31e-2 | 0.177 | −0.124 | 0.977 | 1.000 | 0.163 | 3.1e3 |
| 1 | 3.92e-2 | 0.194 | −0.158 | 0.992 | 1.000 | 0.388 | 7.3e3 |
| 2 | 4.65e-2 | 0.208 | −0.190 | 1.000 | 1.000 | 0.526 | 9.4e2 |

N=4e4 · N=1.6e5 결과가 나오면 같은 표로 비교한다.

## C2 중간 결과 — 예산은 야코비안을 고치지 못한다 (2026-09-21 15:2x KST)

`logs/jacpsd_N{10000,40000}_a1.log`, D2 σ 격자, n=128/점, 동일 진단 코드:

| 예산 | asym(Jr) 범위 | lmin(Jr) | f(lmin<0) | shift 중앙값 |
|---|---|---|---|---|
| N=1e4 | 0.177 ~ 0.208 | −0.124 ~ −0.190 | 0.977 ~ 1.000 | 0.16 ~ 0.53 |
| **N=4e4** | **0.177 ~ 0.217** | −0.137 ~ −0.199 | **0.992 ~ 1.000** | 0.74 ~ 1.97 |
| (정확 GMM 참조) | **2.0e-16** | **+1.9e-3** | **0.000** | **0.000** |

**데이터를 4배 늘려도 비대칭과 부정부호 비율이 전혀 개선되지 않는다.** 이는 표본 수 부족이 아니라
**구조적 결함**이다: denoising score matching 의 목적함수는 score 의 *값*만 구속하고 *미분*은
구속하지 않으므로, 학습된 장이 보존장(conservative field)이 될 이유가 없고 데이터를 늘려도
그 성질은 생기지 않는다.

**이것이 Stage C 의 예측을 바꾼다**: V0(예산만 올린 모델)은 D1 게이트 네 개를 전부 통과했지만
(GD 0.0718 포함) 수신기에서는 여전히 발산할 것으로 예상된다. 확증 실행에 V0 을 반드시 포함해
이것을 **측정**한다 — 가정하지 않는다. 예측이 빗나가면 그대로 보고한다.

정정된 논문 주장의 최종 형태:
> 사전 등록된 네 게이트를 전부 통과해도(GD 0.072 포함) 학습 prior 는 EP 수신기에서 쓸 수 없다.
> L2 형 게이트는 고유값의 **부호**를 구속하지 못하기 때문이다. 그리고 이 결함은 표본 수로
> 해소되지 않는다 — 데이터 4배에도 비대칭은 0.18~0.22 로 불변이다. 해법은 구조적이어야 한다:
> 제약집합으로의 투영(V1), 2차 구조를 요구하지 않는 site(V4), 또는 기울기장 매개화(V2).

---

# Stage C — D1 확증 실행 결과 (2026-09-21 16:30 KST) · **내 예측 3개 중 2개가 틀렸다**

사전 등록: `10_SPEC_stageC.md` §6b (커밋 8cdbe31, **결과 관측 전**).
체크포인트 `ckpt/sx_N160000_D1.pt` (GA/GB/GC/GD 전부 PASS). raw 336개 → `results/tables_D1_C.txt`.
사전 등록 `tables_D1.txt` 는 불변이고 `M-ours-dscore` 는 거기서 BLOCKED 유지 (runner 가
사전 등록 게이트 파일에 PASS 기록이 없음을 확인하고 자동으로 그렇게 했다).

## 기록해 둔 예측과 실제

| # | 예측 (§6b, 결과 보기 전) | 실제 |
|---|---|---|
| 1 | V0 는 게이트 통과에도 **발산하거나 크게 나쁠 것** | **틀림.** 발산하지 않았고 비-genie 최고 성능 |
| 2 | V1·V4 는 정상 동작할 것 | 맞음 (단 V4 는 V0 보다 나쁘다) |
| 3 | D1 에서 학습 prior 가 **GMM 을 이기지 못할 것** | **틀림.** GMM 과 동률이고 정확 score oracle 과 동률 |

## C5 (Tp=3), 앵커 `R2-ours-G` 기준 짝지음 부호검정

| 대상 | 3점 | pooled | 판정 |
|---|---|---|---|
| `M-ours-score` (정확 score **oracle**) | 3/3 | 89:38, p=7.0e-06 | 유의 |
| **`M-ours-dscore-C-V0`** (행렬 site) | **3/3** | **96:34, p=4.9e-08** | **유의** |
| `M-ours-dscore-C-V1` (PSD 투영) | 3/3 | 96:34, p=4.9e-08 | 유의 |
| `M-ours-dscore-C-V4` (스칼라 site) | 0/3 | 76:57, p=0.12 | 무의미 |
| `M-ours-bstar-scalar` (대조군) | 0/3 | 78:51, p=0.022 | 무의미 |

C2 (Tp=4): `R2-ours-G → V0` −3 dB 60:16 p=3.9e-07, pooled 72:21 p=1.0e-07.
**`V0 → R6-exactEP` 7:8 p=1.0** — 정확 EP 상한과 통계적으로 구별되지 않는다.
C1 (Tp=2): `V0 → R6-exactEP` 92:72 p=0.14, 역시 동률.

## 두 가지 구조적 관찰

**(1) V0 와 V1 의 수치가 거의 모든 비교에서 완전히 동일하다** (96:34/96:34, 60:16/60:16,
107:56/107:56). PSD 투영이 아무것도 바꾸지 않았다 = **D1 에서는 야코비안이 이미 유효했다.**
직접 측정으로 확인 (`logs/jacpsd_D1_N160000.log`, D1 σ 격자, n=128):

| | D1 N=1.6e5 | D2 N=1e4 | D2 N=4e4 | 정확 GMM |
|---|---|---|---|---|
| asym(Jr) | **1.9e-3** | 0.177~0.208 | 0.177~0.217 | 2.0e-16 |
| lmin(Jr) | **+0.94** | −0.124~−0.190 | −0.137~−0.199 | +1.9e-3 |
| **f(λmin<0)** | **0.000** | 0.977~1.000 | 0.992~1.000 | 0.000 |
| shift 중앙값 | **3.4e-13** | 0.16~0.53 | 0.74~1.97 | 0.000 |

**(2) D2 와 정반대로 D1 에서는 스칼라 site 가 손해다.** `M-ours-bstar → V4` C1 80:144 p=2.3e-05
(첫 arm 이 2/3 점에서 우세), C5 27:54 p=0.0036. GMM 대조군도 같은 방향
(`M-ours-bstar → M-ours-bstar-scalar` C1 85:110 p=0.085).
= 스칼라화는 **학습 prior 전용 이득이 아니라 site 선택의 효과**이며, 야코비안이 유효할 때는 손해다.

## 아직 가르지 못한 교란 — 다음 측정이 결정한다

D1 의 야코비안은 유효하고 D2 의 것은 무효인데, 두 조건이 **testbed 와 예산** 양쪽에서 다르다
(D1 은 N=1.6e5, D2 측정은 N=1e4·4e4). **D2 N=1.6e5 야코비안 측정이 이것을 가른다**:
- 여전히 부정부호 → **testbed 효과**. D2 의 희소 specular 밀도는 사실상 저차원 다양체라
  Hessian 에 큰 음의 곡률 방향이 있고, 불완전한 학습 score 가 부호를 틀리기 쉽다.
- 유효해짐 → **예산 효과**. 그 경우 "예산은 야코비안을 고치지 못한다"는 내 앞선 진술은
  N=4e4 까지만 본 성급한 일반화였으므로 그대로 정정한다.

## 범위 주의 (과대주장 금지)

- 예산 비대칭: 학습 arm N=1.6e5 vs D1 GMM 적합 N=1e4. §6b 에 따라 `M-ours-bstar → V0` 는
  1차 비교로 쓰지 않는다 (어차피 동률이라 주장에 쓸 것도 없다).
- 공정한 비교는 **V0/V1/V4 3자**(같은 모델·같은 예산·같은 수신기, site 만 다름)이며,
  D1 결과는 **V0 ≈ V1 > V4**.
- D1 은 계측기 testbed 이고 참 prior 가 격자 GMM 으로 정의된 순환 구조다. 여기서의 승리가
  주장 testbed D2 의 결과를 보증하지 않는다.
