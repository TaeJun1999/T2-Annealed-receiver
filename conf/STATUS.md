# STATUS — conf 실험

> Claude Code가 이 파일을 계속 덮어쓴다. **매 phase 종료 시 + 최소 30분마다 갱신 후 commit & push.**

## 세션 설정 (요청대로 첫 줄에 기록)

- 모델 : **Opus 5 (1M context)** — `claude-opus-5[1m]`. 요구사항(Opus) 충족.
- effort / thinking : 세션 내부에서 읽거나 바꿀 수 없는 클라이언트 측 설정이라 **모델이 확인·변경할 수 없다.**
  사용자가 Max 가 아니면 `/model` 에서 올려야 한다. 이 이유로 작업을 멈추지는 않는다(운영 모드 §1).
- 시작 시각 : 2026-09-20 00:22 KST

## 계산 자원 (시작 시 측정)

- GPU : NVIDIA RTX PRO 6000 Blackwell Server Edition x 6, 각 97887 MiB, **6장 전부 0 MiB 사용 = 전부 가용**.
  드라이버 580.173.02 / CUDA 13.0 / Compute Mode Exclusive_Process.
- CPU : 192 코어. RAM 1007 GB (사용 12 GB). 디스크 `/home/HTJ` 6.5T 중 463G 사용 (8%).
- torch 2.14.0+cu130, `torch.cuda.is_available() = True`, device_count 6, `get_arch_list()` 에 `sm_120` 있음.
- numpy 2.5.2 / scipy 1.18.1 / python 3.12.14 (`~/miniforge3/envs/torch`).
- 계획 : 수신기 실행 = CPU 멀티프로세스(192코어, `--jobs` 미지정). score 학습 = GPU(cuda).

---

갱신 시각 : 2026-09-20 13:52 KST
경과 시간 : 3시간 30분
현재 phase : **Stage A 사실상 완료(A6 끝) · A5 사다리 L5 진행 중 · Stage B B1/B2 완료, B3 진행 중**

## 사다리 — 12시도 12실패. **아키텍처 축이 결정적이었다.**

| 칸 | arch | GB (<=5%) | GC (<=0.15) | GD (<=0.20) | GD_trace |
|----|------|-----------|-------------|-------------|----------|
| L1 a1~a3 | mlp/ve | 9.0~14.2% | 0.702~0.784 | 0.376~0.467 | 0.081~0.159 |
| L2 a1~a3 | mlp/vp | 9.5~14.2% | 0.684~0.807 | 0.375~0.468 | 0.100~0.176 |
| L3 a1~a3 | mlp/angle | 9.5~14.6% | 0.718~0.785 | 0.386~0.472 | 0.098~0.168 |
| **L4 a1** | **conv** | **2.6% PASS** | **0.318** | 0.223 | 0.029 |
| **L4 a2** | **conv** | **3.5% PASS** | 0.415 | 0.241 | 0.159 |
| **L4 a3** | **conv** | **2.7% PASS** | 0.616 | **0.2053** | 0.035 |

- MLP 9회는 폭·깊이·학습률·파라미터화·도메인을 다 바꿔도 GC 0.68~0.81 에서 못 움직였다.
  **conv 로 바꾸자 GB 가 통과하고 GC 가 0.318, GD 가 0.205 까지 내려왔다.** 하이퍼파라미터가 아니라
  아키텍처가 구속 조건이었다. L1/L2 에서 멈췄다면 "데이터 예산 한계"라고 잘못 결론냈을 것이다.
- **L4 a3 의 GD = 0.205256, 기준 0.20 — 2.6% 차이로 미달.** 같은 체크포인트의 GD_trace 는 0.035 라
  느슨한 해석이었다면 통과했다. 기준은 어떤 체크포인트가 생기기 전에 동결했고 **조정하지 않는다.**
- L3(각도 도메인)의 게이트 점수가 L2(픽셀)보다 **나빴다**(4.793 vs 4.511) -> 2D-DFT 입력 표현은
  이 prior 에 도움이 안 된다. L4/L5 는 픽셀 도메인을 상속.
- 남은 것: L5(conv 백본 위 flow matching), L6(VAE).

## 완료
- **A6 D1 전 arm 실행 완료** (C1~C4, n=640, raw 320개, 3.6분). 표 A/B/C/D 생성.
  C1 headline @9dB: genie 0.000 < **bstar 0.027 ~ exactEP 0.030** < score 0.039 < R2 0.067
  < gmm32 0.091 < R1 0.148 << R4-scvamp 0.528 < R3 0.611 < R4-llr 0.856
  - b*(kron)가 참 prior EP oracle 과 사실상 동률. 문자 그대로의 K=32 full-cov GMM 은 Gaussian 보다 나쁘다.
  - R4-llr 은 SNR 이 오를수록 BLER 증가(0.677@3dB -> 0.933@15dB) = period-2 한계순환. 그대로 보고.
- **B1 T2d PASS** (L 전 구간 층화, 측정 vs 해석해 5.0e-3, worst CI_hi-2 = -0.2518).
- **B2 D2 GMM 재적합 완료** (23.4분, 120 EM runs). 검증 우도만으로 b* = kron K=128.
  D1 대비 과적합 격차가 7~10배 증가(8x4 kron K=64: gap 0.497 -> 2.080) = testbed 가 실제로 어려워졌다는 2차 증거.
- **B3 sigma 재측정 완료**: D2 격자 nu_q in [2.19e-3, 1.43], sigma_t in [3.31e-2, 8.45e-1].

## 진행 중
- A5 L5/L6 (GPU 0)
- **B2 확장: kron K=256 적합** — b* 가 사전 등록 격자 최상단(K=128)에 놓였고 ll_val 이 경계에서도
  단조 개선 중이라, GMM 을 잘라내지 않기 위해 확장(검증 우도만, BLER 미사용).
  이는 **우리 주장에 불리한 = 보수적** 방향이다. full 계열은 K=32 정점 후 붕괴라 확장 안 함.

## BLOCKED
- 없음

## DECISIONS 누적
- 24건

## 마지막 커밋
- (이 커밋)
